<resources>
    <!-- Base application theme. -->
    <style name="Theme.BikeSorocaba" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/yellow_A400</item>
        <item name="colorPrimaryDark">@color/yellow_600</item>
        <item name="colorAccent">@color/yellow_900</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/black</item>
    </style>

</resources>
